{
  "name": "uc-ingest-admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply uc-ingest --local",
    "migrate:remote": "wrangler d1 migrations apply uc-ingest --remote"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
